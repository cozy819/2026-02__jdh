---
title: 교과서 이미지 제작 가이드
tags: [장대현중고등학교, 정보교육, 교과서, 이미지]
status: 작성중
created: 2026-08-12
updated: 2026-09-11
---

# 교과서 이미지 제작 가이드

상위 문서: [[00 정보와 디지털 문해력 - 수업구성]] · [[08 교과서 집필 기준]]

`교과서_배포/`의 HTML에 들어갈 그림의 **위치, 목적, 생성 프롬프트**를 모아 둔 문서다. 그림을 만들 때마다 이 문서를 갱신한다.

---

## 0. 담당과 작업 경계

- Claude는 교과서 원고와 HTML 집필을 담당한다.
- Codex는 교과서 이미지 작업을 담당한다.
  - 본문과 HTML을 확인해 이미지가 실제로 필요한지 판단한다.
  - AI 생성 이미지의 프롬프트를 설계하고 이미지를 생성한다.
  - 라벨이 많은 개념도는 SVG로 직접 제작한다.
  - 결과물을 지정된 파일명으로 `assets/img/`에 배치한다.
  - 파일 크기, 화면 표시와 A4 인쇄 결과를 확인한다.
  - 생성·수정·배치가 끝날 때마다 이 가이드의 진행 체크리스트를 갱신한다.
- 실제 프로그램 화면도 생성 이미지로 만들 수 있다. **판단 기준은 캡처인지 아닌지가 아니라 학생이 보고 이해할 수 있는가**다. 실물과 똑같으면 좋지만 반드시 똑같아야 하는 것은 아니다. (2026-09-04 개정. 그전에는 "생성 이미지로 대체하지 않는다"였다.)
    - 한 그림 안에서 **서로 어긋나는 상태를 그리지 않는다.** 예: 커널 미선택 표시가 떠 있는데 셀에 결과가 나와 있는 화면.
    - 학생이 그 화면에서 실제로 눌러야 하는 것(버튼 이름, 메뉴 위치, 파일 이름, 폴더 구조)은 실제와 맞춘다. 버전 번호·상태바 같은 잔가지는 달라도 된다.
    - 그래도 캡처가 편하고 정확한 자리(설치 진행 화면, 오류 화면)는 그대로 캡처한다. 캡처는 교실과 같은 환경에서 찍고, 개인정보 가림과 번호·테두리는 배치할 때 얹는다.
- 원고나 HTML의 내용이 변경되면 기존 이미지 목록과 목적도 다시 검토한다. 자리표시자가 있다는 이유만으로 이미지를 그대로 만들지 않는다.
- **프롬프트는 이 문서와 교과서 HTML 두 곳에 둔다.** HTML에서는 `<figure>` 바로 위에 HTML 주석으로 넣어 브라우저에는 안 보이고 소스에만 보이게 한다. 그림 자리와 프롬프트가 같은 파일에 있어야 ChatGPT에 넘기기가 빠르다. 이 문서는 전체 목록과 이력을 맡고, HTML 주석은 그 자리에서 바로 쓰는 사본이다. 프롬프트를 고치면 양쪽을 같이 고친다.

### 제작 순서

1. Claude가 해당 차시의 HTML 초안을 확정한다.
2. Codex가 본문, 자리표시자와 이미지 가이드를 대조한다.
3. CSS·SVG·화면 캡처·AI 생성 중 적절한 방식을 선택한다.
4. AI 생성이 필요한 이미지는 라벨 없이 만들고 시각적 정확성을 검토한다.
5. 필요한 라벨을 별도 방식으로 얹고 PNG로 내보내거나 SVG로 완성한다.
6. 지정된 ID로 저장하고 스크롤·슬라이드·인쇄 모드에서 확인한다.

현재 HTML의 「이미지 생성 요청」 주석이 가리키는 **19개가 모두 완성되어** `assets/img/`에 배치되어 있다. 그 밖의 화면 캡처·그래프·SVG 도식도 HTML 참조 누락이 없다. 도식은 수정 가능한 SVG 원본과 HTML 배포용 PNG를 함께 두고, 장면 일러스트는 원본 PNG와 편집 가능한 HTML·PPT 라벨을 분리한다. (`01-2-02-cli-gui`는 2026-08-19, `01-2-03-io-flow`는 2026-08-29 설명 개편으로 폐기했다.)

> [!important] 6-2~8회에는 추가 이미지를 만들지 않는다
> 2026-09-11에 6-1 도입 장면 `06-1-01-endless-list`가 추가되었다. 나머지 6-2~8-2는 입력·동작·결과를 글과 `.vflow`·`.iot`·`.kv`·`.stack` 같은 CSS 도식으로 설명하므로 별도 이미지를 추가하지 않는다.
> 이 차시들에 그림을 추가할 때는 **본문에서 도식을 대체하지 말고**, 새 `figure.fig`를 넣을 자리를 먼저 이 문서에 등재한 뒤 제작한다.

> [!warning] 2026-08-19 개편으로 차시 번호가 바뀌었다
> 옛 4-1(흐름·오류)이 **3-2**로, 옛 5-1·5-2가 **4-1·4-2**로, 옛 6-1·6-2가 **5-1·5-2**로 내려왔다. 이미 완성된 10개(`00-01` ~ `03-1-01`)는 ID가 그대로이므로 **다시 만들 필요가 없다.** (2026-09-04 3주차 재설계로 `03-1-01-variable-label`만 폐기되고 `03-1-01-result-location`으로 교체되었다.) 다만 `00-01-course-map`은 학기 구조가 1주 / 2~5주 / 6~8주 / 9~13주 네 단계로 바뀌어 **SVG와 PNG를 갱신했다.**

---

## 1. 작업 방식

### 파일을 넣기만 하면 된다

HTML은 이미 `assets/img/<ID>.png`를 참조하도록 작성되어 있다. **HTML을 고칠 필요가 없다.**

```
교과서_배포/assets/img/01-2-01-human-computer.png   ← 이 이름으로 저장하면 끝
```

파일이 없으면 `textbook.js`의 `imgPlaceholder()`가 자동으로 안내 상자를 보여 준다. 지금 브라우저로 열어 보면 점선 상자에 `IMG 01-2-01-human-computer`와 설명이 표시된다. 파일을 넣고 새로고침하면 그 자리에 그림이 들어간다.

### 브랜드

2026-08-19부터 교과서는 **바이브코딩연구소 2026 브랜드**를 따른다. 새로 만드는 그림도 같은 규격에 맞춘다. (원본: `2608-02_harness_v2/templates/brand/문서-스타일-스펙.md`)

| 항목 | 값 |
|---|---|
| 강조 | 시그널 오렌지 `#FF5C00` — 화면의 5% 이하 |
| 본문·선 | 카본 `#1B1D21` |
| 음영 | 쿨 그레이 `#F1F2F4` — 다른 색으로 면을 채우지 않는다 |
| 보조 텍스트 | 스틸 `#6C7078` |
| 긍정 상태 | 틸 `#0E7C86` |
| 글꼴 | 국문 IBM Plex Sans KR · 영문/수치 Archivo · 코드 IBM Plex Mono |

- **그라데이션과 그림문자(이모지)를 넣지 않는다.**
- 모서리는 각지게 둔다(둥근 모서리 없음).
- 구조를 이루는 선과 테두리는 **회색**(`#6C7078` · `#C7C9CE`)으로 둔다. 선까지 주황으로 칠하면 강조가 사라진다.
- 하지 말 것을 표시할 때만 `#C24A00`을 쓴다. 브랜드에 빨강·노랑은 없다.
- **화살촉은 크기를 고정한다.** `<marker>`에 `markerUnits="userSpaceOnUse"`를 넣고 `markerWidth`/`markerHeight`를 24 안팎으로 둔다. 기본값이면 선이 굵어질수록 화살촉이 같이 커져 도형을 덮는다. 또 `marker-end`는 경로의 **마지막 꼭짓점에만** 붙으므로, 갈래마다 화살촉이 필요하면 `<path>`를 나눈다.
- 2026-08-24에 인디고 팔레트로 남아 있던 6개를 **모두 VCL 팔레트로 다시 칠했다.** 이제 그림 전체가 브랜드와 맞는다. (`01-2-01-human-computer`와 `02-2-01-vscode-4`는 SVG 원본이 없어 예외로 둔다.)

> [!warning] 문장 하나를 그린 그림은 만들지 않는다
> `01-1-01-literacy`(동심원 4단)를 2026-08-24에 폐기했다. 동심원이 주장하는 것은 **“A가 B를 포함한다”** 한 문장뿐이어서, 그 문장을 글로 쓰면 그림이 더할 것이 없었다. 게다가 **대상(정보)과 능력(문해력)을 한 축에 섞어** 범주가 어긋났고 포함 순서도 뒤집혀 있었다.
> 그림은 **글로 하기 어려운 것**(구조·흐름·비교·수치)에만 쓴다. 낱말의 상하 관계는 용어 상자의 `경계` 줄이나 README §5의 CSS 도식(`.stack`)으로 쓴다. **CSS 도식은 한중 병기가 되고 이미지처럼 번역이 막히지 않는다.**

### 형식

- **PNG** 권장. 사진풍 일러스트는 JPG도 무방하다.
- 가로 폭 **1600px 이상**. 인쇄(A4)에서 흐려지지 않을 정도.
- 파일 하나가 **1MB를 넘지 않도록** 압축한다. 교실 노트북이 저사양이고 한 쪽에 여러 이미지가 들어갈 수 있다.
- 배경은 **흰색 또는 아주 옅은 회색**. 교과서 페이지가 흰 바탕이다.

---

## 2. 공통 스타일 (프롬프트 앞에 붙일 것)

교과서 전체가 한 벌로 보여야 하므로 색과 톤을 고정한다. **블록이 두 개다. 항목마다 하나만 고른다.**

### A. 사실적 삽화 — 3주차 이후 기본

사람·물건·장면이 나오는 개념 삽화는 전부 이쪽이다. **도해보다 사실적인 그림으로 간다**는 것이 3주차에 정한 방향이다 ([[00 결정 로그]]).

```
Style: warm realistic editorial illustration for a school textbook.
Real objects and a real scene, hand-drawn or painterly feel, soft natural light.
NOT a flat vector diagram, NOT boxes-and-arrows, NOT a wireframe, NOT an infographic.
Restrained palette — muted neutrals and warm grays for everything,
signal orange #FF5C00 ONLY on the single element the reader should look at first
(one small area, well under 5% of the image).
Plain near-white background #ffffff, uncluttered, generous empty space.
No gradients on flat panels, no drop shadows, no 3D render look, no emoji,
no dark background, no busy detail. Calm, warm, legible when printed in a textbook.
Aspect ratio: <각 항목의 비율>
```

### B. 플랫 벡터 도해 — 값·순서·구조를 정확히 보여야 할 때

정확한 값이나 단계 순서가 그림의 요점인 항목만 이쪽이다. 라벨이 많으면 이미지 대신 SVG 로 간다.

```
Style: clean modern educational textbook diagram, flat vector style,
generous white space, square corners, thin consistent line weight.
Color palette — signal orange #FF5C00 only for the key point,
carbon #1B1D21 for outlines, cool gray #F1F2F4 for panels,
steel #6C7078 for secondary elements, teal #0E7C86 for positive states,
white background #ffffff.
No gradients, no drop shadows, no 3D, no photorealism, no emoji,
no dark background, no clutter. Calm, precise and legible.
Aspect ratio: <각 항목의 비율>
```

**A 와 B 를 같이 붙이지 않는다.** 「사실적으로」와 「no photorealism」이 한 프롬프트에 같이 들어가면 모델이 둘 중 아무거나 고른다.

### ⚠️ 텍스트는 이미지에 넣지 않는다

생성형 이미지 모델은 **한글을 거의 정확히 그리지 못한다.** 글자가 깨진 그림은 교과서에 쓸 수 없다.

그래서 각 항목에 **라벨** 칸을 따로 두었다. 처리 방법은 둘 중 하나다.

| 방법 | 언제 | 어떻게 |
|---|---|---|
| **A. 라벨 없이 생성 후 얹기** | 사람·장면 일러스트 | 프롬프트에 `no text, no letters, no labels`를 넣어 그림만 만들고, Figma·Keynote·PowerPoint에서 한글 라벨을 얹어 PNG로 내보낸다. |
| **B. 그림 대신 SVG/CSS** | 도식·개념도 | 아래 표에서 **SVG 권장**으로 표시한 항목. 라벨이 많은 도식은 이미지보다 인라인 SVG가 낫다. 확대해도 깨지지 않고, 인쇄가 선명하고, 나중에 글자만 고칠 수 있다. |

모든 프롬프트 끝에 다음을 붙인다.

```
no text, no letters, no numbers, no labels, no watermark
```

---

## 3. 이미지 목록

전체 17개. **모두 제작·배치 완료**했다. 2026-08-24에는 확정된 HTML 본문과 대체 텍스트를 다시 읽고, 비어 있던 03-2~05-2 도식 7개를 바이브코딩연구소 2026 브랜드로 제작했다. 정확한 값·흐름·순서가 중요한 항목이므로 생성형 이미지 대신 SVG로 설계하고 같은 비율의 PNG로 렌더링했다.

| ID | 위치 | 종류 | 우선순위 |
|---|---|---|---|
| `00-01-course-map` | index.html · 학습 지도 | SVG 완료 | ★★☆ |
| `01-1-01-literacy` | 01-1.html · 네 낱말의 뜻 | SVG 완료 | ★★☆ |
| `01-2-01-human-computer` | 01-2.html · computer는 사람이었다 | **AI 생성 완료** | ★★★ |
| `01-2-02-scope` | 01-2.html · 코딩·프로그래밍·개발 | SVG 완료 | ★☆☆ |
| `01-2-04-cpu-ram-storage-analogy` | 01-2.html · CPU·RAM·저장장치 비유 | **AI 생성 완료** | ★★★ |
| `02-1-01-notebook-order` | 02-1.html · Notebook의 함정 | SVG 완료 | ★★★ |
| `02-2-01-vscode-4` | 02-2.html · VS Code 네 곳 | **화면 캡처 완료** | ★★★ |
| `02-2-02-venv` | 02-2.html · 가상환경 | SVG 완료 | ★★☆ |
| `02-2-03-three-pythons` | 02-2.html · 인터프리터와 커널 | SVG 완료 | ★★★ |
| `03-1-01-result-location` | 03-1.html · 코드 바로 아래에서 결과를 본다 · PPT 2번 | **AI 생성 완료** | ★★★ |
| ~~`03-1-01-variable-label`~~ | 3주차 재설계로 폐기 | — | — |
| `03-2-01-control-flow` | 03-2.html · 실행 흐름 | SVG 완료 | ★★★ |
| `03-2-02-list-index` | 03-2.html · 위치 번호는 0부터 | SVG 권장 | ★★☆ |
| ~~`04-1-01-chart-types`~~ | 4주차 재설계로 폐기 · `_retired/` | — | — |
| ~~`04-1-02-axis-trick`~~ | 4주차 재설계로 폐기 · `_retired/` | — | — |
| ~~`04-1-03-same-mean`~~ | 4주차 재설계로 폐기 · `_retired/` | — | — |
| ~~`04-2-01-notebook-flow`~~ | 4주차 재설계로 폐기 · `_retired/` | — | — |
| `04-1-00-kernel-picker` | 04-1.html · 04 폴더의 `.venv` 커널 선택 | **AI 화면 이미지 완료** | ★★★ |
| `04-1-02-library-shelf` | 04-1.html · 시판 재료로 자기 요리를 완성한다 | **AI 생성 교체 완료** | ★★★ |
| `04-1-03-matplotlib-error` | 04-1.html · 오류 마지막 줄을 AI에게 전달 | **화면 이미지 완료** | ★★★ |
| `04-2-03-hidden-questions` | 04-2.html · 표를 보면 물어볼 것이 생긴다 | **AI 생성 완료** | ★★★ |
| `05-2-01-submission-layout` | 05-2.html · 제출물 구성 | SVG 권장 | ★★★ |

---

### 03-1-01-result-location ★★★

- **위치** — `03-1.html` · 1. 코드 바로 아래에서 결과를 본다 / `03-1` PPT 2번 슬라이드. **한 파일을 둘이 같이 쓴다.**
- **캡션** — 그림 3-1-1. 같은 코드인데 결과가 나오는 곳이 다르다. 왼쪽은 아래쪽 터미널, 오른쪽은 코드 바로 밑.
- **목적** — 2-2까지 결과는 아래쪽 터미널에만 나왔다. 3-1에서 무엇이 달라지는지를 **글이 아니라 그림 한 장으로** 먼저 보여 준다. 이 차시의 도입 장면이다.
- **비율** — 16 : 9 (좌우 2분할)
- **판단** — **AI 생성 완료 (2026-09-04).** ChatGPT로 만들었다. 1672 × 941 · 753KB. 창 제어 버튼이 오른쪽 위에 제대로 나왔고, 코드·결과는 막대로 나와 글자 뭉개짐이 없다. 교과서 `assets/img/`와 PPT 생성스크립트의 `img/`에 같은 파일을 둔다.
- **환경** — **반드시 Windows 창으로 만든다.** 학생 컴퓨터가 전부 Windows다. 창 오른쪽 위의 `─ □ ✕` 세 버튼이 Windows 신호이고, macOS의 왼쪽 위 동그라미 세 개가 나오면 안 된다.
- **주의** — 이 그림의 내용은 코드와 결과가 **어디에 놓이는가**다. 생성형 이미지는 글자를 자주 뭉개므로 **코드와 결과는 막대(wireframe bar)로 두고**, 실제 글자가 필요하면 HTML·PPT에서 얹는다. 뭉개짐 없이 잘 나오면 그때 글자 버전으로 다시 만들어도 된다.
- **대안** — 결과가 만족스럽지 않으면 SVG로 직접 그린다. 이 차시의 다른 도식도 대부분 SVG다.
- **합성 UI 아님** — 이 그림은 실제 화면 캡처를 대신하지 않는다. 개념을 먼저 보여 주는 도해이고, 실제 VS Code 화면은 바로 뒤 슬라이드의 캡처가 맡는다. 그래서 사진처럼 만들지 않고 **한눈에 그림인 것이 보이도록** 평면 벡터로 만든다.

**프롬프트** — 맥락 머리말과 함께 보낸다

```
[맥락 — 프롬프트 앞에 이것도 같이 보낸다]

장대현중고등학교 「정보와 디지털 문해력」 정규 교과의 3회차 첫째 차시(3-1)에 쓸
개념 도해다. 차시 제목은 「코드 바로 아래에서 결과를 본다」.

- 학생: 중3~고2, 코딩 수업이 처음이다. 컴퓨터는 전원 Windows.
- 지난 차시(2-2)에서는 .py 파일을 실행하면 결과가 편집기 아래쪽 터미널 칸에만
  글자로 나왔다.
- 이번 차시에서 Jupyter Notebook을 준비해, 코드 한 줄 바로 밑에 결과가 붙어
  나오게 만든다.
- 이 그림은 그 차이를 차시 도입부에서 한 장으로 보여 주는 것이다. 설명 문장은
  쓰지 않고, 그림만으로 "결과가 나오는 곳이 달라졌다"가 보여야 한다.
- 파일명: 03-1-01-result-location · 교과서 HTML과 강의 PPT가 같은 파일을 쓴다.
- 바이브코딩연구소 2026 브랜드를 따른다. 그라데이션·이모지·그림자를 쓰지 않고,
  주황(#FF5C00)은 강조에만 아주 적게 쓴다.

[프롬프트]

A clean side-by-side comparison of two code editor windows, drawn as a flat
vector illustration, not a photograph and not a screenshot.

Both windows are Windows-style desktop application windows: a rectangular title
bar with three small controls in the TOP-RIGHT corner — a minimise dash, a
maximise square, and a close cross. Do NOT put three round coloured dots in the
top-left; this must not look like macOS.

LEFT WINDOW — the editor area fills the upper two thirds and holds three short
lines of code. Below it, separated by a clear horizontal divider, a darker panel
occupies the bottom third; a single short line of output sits alone in that
bottom panel, far away from the code that produced it. Draw a thin curved arrow
from the code down to that bottom panel to show the distance the eye must travel.

RIGHT WINDOW — the same three lines of code, but the window is divided into
stacked blocks. Each block holds one line of code with its own small result
sitting immediately underneath it, inside the same block, only a few pixels
below the code. No separate bottom panel at all. Three such code-and-result
pairs stack down the window.

A single wide arrow points from the left window to the right window in the gap
between them.

Style: flat vector, thin uniform 2px strokes, generous white space, no shadows,
no gradients, no 3D, no perspective. Palette strictly limited to: white
background, light cool grey #F1F2F4 for panel fills, #E0E3E7 for divider lines,
near-black #1B1D21 for window frames and code lines, and orange #FF5C00 used
ONLY for the result lines and the arrows — nothing else is orange.

Render the code and output lines as abstract horizontal bars of varying length,
like a wireframe placeholder, NOT as readable characters.
no text, no letters, no numbers, no labels, no watermark
```

---

### 01-2-04-cpu-ram-storage-analogy

- **위치** — `01-2.html` · 2-1 「CPU는 일하고, RAM은 펼쳐 두고, 저장장치는 보관한다」
- **캡션** — 그림 1-2-3. 사람은 CPU, 책상은 RAM, 책장과 서랍은 저장장치에 가깝다.
- **목적** — 2026-08-25 첫 수업에서 구두로 사용한 `일하는 사람–책상–책장` 비유를 시각화한다. 부품 사양을 외우게 하지 않고 `처리·임시 작업·장기 보관`의 역할을 구분한다.
- **비율** — 16 : 9
- **판단** — **AI 장면 일러스트 확정.** 사람·책상·책장과 서랍의 공간 관계가 한눈에 보여야 하며, 영문 약어 라벨은 이미지에 굽지 않고 HTML과 PPT에서 편집 가능한 글자로 얹는다.
- **구성** — 책상 앞에서 계산하는 한 사람, 지금 쓰는 자료가 펼쳐진 넓은 책상, 뒤편에 파일을 보관하는 책장과 서랍. 라벨은 사람 위 `CPU`, 책상 위 `RAM`, 책장·서랍 위 `SSD · HDD`만 사용한다.
- **주의** — CPU가 사람처럼 스스로 생각한다는 뜻이 아니다. RAM이 크면 CPU 자체가 빨라진다는 뜻도 아니다. 입력과 출력은 아래 역할 표에서 설명하며, 장면을 억지로 직선 흐름으로 만들지 않는다.

### ~~01-2-03-io-flow~~ — 대체됨 (2026-08-29)

- **대체 이유** — `입력 → CPU → RAM → 저장 → 출력`의 한 줄 흐름은 CPU와 RAM이 작업 중 계속 값을 주고받는 관계를 오해시킬 수 있다. 첫 수업에서 실제로 사용한 작업실 비유가 초보 학생에게 더 직접적이므로 `01-2-04-cpu-ram-storage-analogy`로 교체했다.

---

### ~~03-1-01-variable-label~~ — 폐기 (2026-09-04)

- **상태** — **사용하지 않는다.** 3주차 재설계로 3-1이 「실습 환경 준비와 비교 결과로 판단하기」가 되면서 변수 절이 사라졌다. 변수는 2-2에서 다룬다. 파일이 남아 있으면 `assets/img/_retired/`로 옮긴다.

---

### 00-01-course-map

- **위치** — `index.html` · 학습 지도 쪽
- **캡션** — 그림 0-1. 한 학기 학습 흐름
- **목적** — 왜 창작을 바로 하지 않고 Python부터 배우는지, 그리고 개발 전에 왜 계획하는 구간이 따로 있는지를 한눈에 보여 준다.
- **비율** — 16 : 6 (가로로 긴 띠)
- **판단** — **SVG 확정.** 회차 번호와 구간 이름이 모두 글자다. `00-01-course-map.svg`를 고치고 다시 렌더링한다.
- **상태** — 2026-08-19 갱신 완료. 학기가 네 단계로 바뀌면서 SVG와 PNG를 모두 다시 만들었다.

**라벨 (현재 SVG에 들어 있는 값)**

```
[1주] 오리엔테이션 — "수업 열기"
[2~5주] Python과 데이터 — "읽는 힘"
[6~8주] 기획과 데이터 설계 — "계획하는 힘"
[9~13주] AI와 서비스 개발 — "만드는 힘"
마일스톤 ⑤ 수행평가 1 · 데이터 시각화
마일스톤 ⑧ 수행평가 2 · 제작 계획서
마일스톤 ⑬ 수행평가 3 · 창작물
```

**고칠 때**

```bash
# SVG 를 수정한 뒤 같은 크기로 다시 렌더링한다
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless --disable-gpu --window-size=1600,600 \
  --default-background-color=FFFFFFFF --hide-scrollbars \
  --screenshot="assets/img/00-01-course-map.png" \
  "file://$PWD/assets/img/00-01-course-map.svg"
```

---

### 01-1-01-literacy

- **위치** — `01-1.html` · 2. 낱말 정의 (이어서)
- **캡션** — 그림 1-1-1. 네 낱말은 서로를 포함하며 넓어진다
- **목적** — 정보·문해력·디지털 문해력·AI 문해력이 별개가 아니라 포개진 개념임을 시각화한다.
- **비율** — 16 : 9
- **판단** — **SVG 권장.** 네 개의 낱말이 모두 라벨이다.

**라벨**

```
바깥 → 안 순서
AI 문해력  /  디지털 문해력  /  문해력  /  정보
가장 안쪽 작은 점: 데이터
```

**프롬프트 (이미지로 만들 경우)**

```
Four concentric rounded rectangles nested inside one another, viewed flat from
the front, with comfortable padding between each layer. The innermost shape is a
small solid dot. Each outer layer is progressively lighter in tone.
Flat vector diagram, indigo #4f46e5 outermost outline, layers filled with
#eef2ff, #f4f4f5, white. Thin uniform strokes. White background, generous margin.
no text, no letters, no numbers, no labels, no watermark
```

---

### 01-2-01-human-computer ★★★

- **위치** — `01-2.html` · 1. computer는 사람의 직업이었다
- **캡션** — 그림 1-2-1. 계산을 직업으로 삼았던 사람들, ‘human computer’
- **목적** — “컴퓨터가 원래 사람이었다”는 이 차시의 도입 장면을 시각적으로 각인시킨다. 라벨이 필요 없어 **AI 생성에 가장 적합**하다.
- **비율** — 16 : 9
- **주의** — 특정 실존 인물을 묘사하지 않는다. 시대 분위기만 전달한다. 여성이 다수 포함되도록 한다(본문에서 여성들의 참여를 언급한다).

**프롬프트**

```
A mid-20th-century computing room seen from a gentle three-quarter angle.
Several people, mostly women, sit along long shared desks arranged in rows.
Each person works with paper calculation sheets, ruled tables and a small
mechanical desk calculator. One person passes a sheet to the person beside them,
suggesting that results are checked by someone else. Large windows on one side,
soft daylight. Warm, dignified, focused atmosphere — this is skilled professional work.
Clean modern editorial illustration, flat vector style with subtle texture,
limited palette: indigo #4f46e5 as the dominant accent, warm neutral grays,
cream paper tones, white background. Thin consistent line weight.
No computers, no monitors, no screens of any kind.
no text, no letters, no numbers, no labels, no watermark
```

---

### ~~01-2-02-cli-gui~~ — 폐기 (2026-08-19)

- **상태** — **사용하지 않는다.** 2026-08-19 설계 개편으로 1-2에서 CLI→GUI 절이 삭제되었다. 파일은 `assets/img/_retired/`로 옮겨 두었으며, 되살릴 일이 없으면 지워도 된다.
- **옛 위치** — `01-2.html` · 5. 인터페이스의 역사
- **캡션** — 그림 1-2-2. 같은 일을 하는 두 가지 방식 — CLI와 GUI
- **목적** — 두 인터페이스가 어떻게 생겼는지 본 적 없는 학생에게 실물 감각을 준다.
- **비율** — 16 : 9 (좌우 2분할)
- **주의** — 왼쪽 CLI 화면에 글자가 들어가야 자연스럽다. **의미 없는 문자 나열이라도 무방하므로**, 예외적으로 텍스트 금지 지시를 완화한다. 다만 한글은 넣지 않는다. 깨진 글자가 신경 쓰이면 방법 A(라벨 얹기)로 처리한다.

**프롬프트**

```
A side-by-side comparison of two computer screens on a plain white background,
separated by a thin vertical divider line.

Left screen: a terminal window with a dark navy background (#0e1524),
filled with several short lines of monospaced light-gray Latin text and a
blinking block cursor. No icons, no images, no windows — only text lines.

Right screen: a light desktop interface with two overlapping rounded windows,
a row of simple flat icons along the bottom, a small folder icon and a mouse
pointer arrow. Soft indigo #4f46e5 accents on window title bars.

Both screens drawn as simple flat rounded rectangles with thin #e4e4e7 borders,
same size, aligned. Clean modern flat vector illustration, no shadows, no 3D.
No Korean characters. No watermark.
```

---

### 01-2-02-scope

- **위치** — `01-2.html` · 5. 코딩·프로그래밍·개발
- **캡션** — 그림 1-2-2. 세 낱말이 가리키는 범위
- **목적** — 세 낱말의 포함 관계를 보여 준다.
- **비율** — 1 : 1 (정사각)
- **판단** — **SVG 권장.** 본문에 이미 같은 내용의 `.stack` 도식이 있으므로 **우선순위가 낮다.** 생략해도 무방하다.

**라벨**

```
바깥 → 안 순서
소프트웨어 개발  /  프로그래밍  /  코딩
```

**프롬프트 (이미지로 만들 경우)**

```
Three concentric circles nested inside one another, drawn flat from the front,
each with generous space between rings. Innermost circle filled solid indigo
#4f46e5, middle ring light indigo #eef2ff, outer ring white with a thin
#e4e4e7 outline. Perfectly centered, white background, wide margins.
no text, no letters, no numbers, no labels, no watermark
```

---

### 02-1-01-notebook-order ★★★

- **위치** — `02-1.html` · 4. Notebook의 함정 — 실행 순서
- **캡션** — 그림 2-1-1. 보이는 순서와 실행한 순서는 다를 수 있다
- **목적** — 이 차시에서 **가장 설명하기 어려운 개념**이다. 문서에 보이는 순서와 실제 실행 순서가 어긋난다는 것을 글로만 설명하면 잘 와닿지 않는다. 그림이 꼭 필요하다.
- **비율** — 16 : 9

**라벨** (방법 A로 얹을 것)

```
왼쪽 제목: 문서에 보이는 순서
오른쪽 제목: 실제로 실행한 순서
각 셀 옆 번호: [1] [2] [3]
오른쪽 아래: "위 셀의 값이 없어서 오류"
```

**프롬프트**

```
Two vertical columns on a white background, separated by a thin divider.

Left column: three stacked rounded rectangles representing notebook cells,
arranged neatly top to bottom, each with a small square badge on its left edge.
Light indigo #eef2ff fill, thin #e0e7ff borders. Calm and orderly.

Right column: the same three rounded rectangles in the same positions, but a
thick indigo #4f46e5 arrow weaves between them in a tangled out-of-order path —
starting at the bottom rectangle, curving up to the top one, then down to the
middle one. The arrow crosses over itself once. One rectangle is outlined in
amber #b45309 to mark a problem.

Flat vector diagram, thin consistent strokes, no shadows, generous white space.
no text, no letters, no numbers, no labels, no watermark
```

---

### 02-2-01-vscode-4 ★★★ (AI 생성 아님)

- **위치** — `02-2.html` · 4.2 VS Code에서 쓸 네 곳
- **캡션** — 그림 2-2-1. VS Code에서 쓰게 될 네 곳
- **목적** — 처음 VS Code를 여는 학생이 화면에서 길을 잃지 않게 한다.
- **판단** — **실제 화면 캡처를 써야 한다.** 생성 이미지는 실물과 달라 오히려 혼란을 준다.

**촬영 방법**

1. 실습에 쓸 노트북에서 `JDH_VibeCoding_자기이름` 폴더를 VS Code로 연다.
2. 왼쪽 탐색기를 펼치고, 아래쪽 터미널을 연 상태로 만든다.
3. 편집기에는 `notebooks/00_setup_test.ipynb`를 열어 둔다.
4. 화면 전체를 캡처한다. 창 크기는 **1600×900 이상**.
5. 다음 네 곳에 **번호 원(①②③④)과 테두리**를 얹는다.
   - ① 탐색기 Explorer
   - ② 편집기 Editor
   - ③ 확장 Extensions (왼쪽 아이콘 줄)
   - ④ 터미널 Terminal
6. 번호 색은 교과서 색과 맞춰 indigo `#4f46e5` / 흰 글자.

**주의**

- 캡처 전에 **파일 경로에 실명이 없는지** 확인한다. 사용자 폴더 이름이 그대로 찍히기 쉽다.
- 열려 있는 다른 탭, 알림, 확장 목록에 개인 정보가 없는지 본다.
- 교실 노트북과 **같은 테마**(기본 Dark 또는 Light)로 통일한다.

---

### 02-2-02-venv

- **위치** — `02-2.html` · 9. 가상환경이 필요한 나머지 세 가지 이유
- **캡션** — 그림 2-2-2. 환경 자체가 아니라 ‘환경을 만드는 기록’을 전달한다
- **목적** — `.venv`를 복사해 주는 것이 아니라 기록 파일을 전달해 재생성한다는, 학생이 가장 헷갈려 하는 지점을 보여 준다.
- **비율** — 16 : 9

**라벨** (방법 A로 얹을 것)

```
왼쪽 위: data-project/.venv → pandas, Matplotlib
왼쪽 아래: web-project/.venv → FastAPI, Uvicorn
가운데 작은 문서: pyproject.toml + uv.lock
오른쪽: 다른 노트북 — uv sync
가로지르는 X 표시 위: .venv 폴더는 전달하지 않는다
```

**프롬프트**

```
A left-to-right process diagram on a white background.

Left side: two separate rounded rectangle containers stacked vertically, each
holding two small pill shapes inside. The containers are clearly separate from
each other, with a visible gap. Light indigo #eef2ff fill, thin #e0e7ff borders.

Center: a small document icon with a folded corner, drawn in indigo #4f46e5.
A thick indigo arrow points from the left containers to this document, and
another arrow continues from the document to the right.

Right side: a simple laptop outline, and inside its screen a rounded rectangle
container being rebuilt — drawn with a dashed indigo outline to suggest
regeneration.

Below the center, a separate small container shape with a red #b91c1c diagonal
cross over it, indicating "not this one".

Flat vector diagram, thin consistent strokes, no shadows, generous white space.
no text, no letters, no numbers, no labels, no watermark
```

---

### 02-2-03-three-pythons ★★★

- **위치** — `02-2.html` · 13. 인터프리터와 커널 선택하기
- **캡션** — 그림 2-2-3. 세 곳이 모두 같은 `.venv`를 가리켜야 한다
- **목적** — **실습에서 가장 자주 실패하는 지점**이다. 세 곳의 설정이 하나로 모여야 한다는 것을 그림으로 못 박는다.
- **비율** — 16 : 9
- **판단** — **SVG 권장.** 세 갈래 이름이 모두 라벨이고, 인쇄에서 선명해야 한다.

**라벨**

```
위 세 개: 터미널 uv run python  /  VS Code 인터프리터  /  Notebook 커널
아래 하나: 회차 폴더의 .venv (예: 03/.venv)
아래 작은 글씨: 세 화살표가 모두 한 곳을 가리켜야 한다
```

**프롬프트 (이미지로 만들 경우)**

```
A converging diagram on a white background. Three rounded rectangle boxes sit
in a row across the top, evenly spaced. From the bottom of each box, a thick
indigo #4f46e5 arrow curves downward and inward, all three meeting at a single
larger rounded rectangle centered below them. The bottom box is filled solid
indigo #4f46e5; the three top boxes are light indigo #eef2ff with thin
#e0e7ff borders. Symmetrical, balanced composition.
Flat vector diagram, thin consistent strokes, no shadows, generous white space.
no text, no letters, no numbers, no labels, no watermark
```

---

### 03-1-01-variable-label ★★★ — 폐기 (2026-09-04)

> [!warning] 3주차 재설계로 이 그림은 쓰지 않는다
> 3-1이 「실습 환경 준비와 비교 결과로 판단하기」로 바뀌면서 변수 절이 사라졌다. 아래 내용은 기록으로만 남긴다.

- **옛 위치** — `03-1.html` · 3. 변수는 값에 붙인 이름이다
- **캡션** — 그림 3-1-1. 변수는 상자가 아니라 값을 가리키는 이름표에 가깝다
- **목적** — 이 차시의 **오개념 1순위**를 정면으로 다룬다. 본문에서 “변수는 상자가 아니라 이름표”라고 말로 바로잡지만, 학생 머릿속에는 이미 상자 그림이 들어 있다. 말보다 그림이 필요한 자리다.
- **비율** — 16 : 9
- **주의** — **상자를 그리면 안 된다.** 값을 안에 담는 그림은 이 페이지가 바로잡으려는 오개념을 다시 심는다. 반드시 **바깥에서 화살표로 가리키는** 구도여야 한다.

**라벨** (방법 A로 얹을 것)

```
왼쪽 위 이름표: price      왼쪽 아래 값: 1200
오른쪽 위 이름표: price    오른쪽 아래 값: 1200(흐리게) / 3000(진하게)
가운데 구분선 위: price = 1200        →        price = 3000
아래 한 줄: 값이 이름표 안으로 들어가는 것이 아니라, 이름표가 값을 가리킨다
```

**프롬프트**

```
Two side-by-side panels on a white background, separated by a thin vertical divider.

Left panel: a small rounded tag shape with a short string, like a luggage tag,
positioned upper left. A thick indigo #4f46e5 arrow leaves the tag and points
down-right to a separate rounded rectangle containing a blank space. The tag and
the rectangle are clearly separate objects with a visible gap between them —
the tag is NOT a container and nothing is inside it.

Right panel: the same tag in the same position, but its arrow now points to a
different rounded rectangle placed lower right. The previously pointed rectangle
is still present but drawn faded in light gray #e4e4e7 with no arrow touching it.

Flat vector diagram, thin consistent strokes, no shadows, no boxes containing
other boxes, generous white space.
no text, no letters, no numbers, no labels, no watermark
```

## 3-b. 3주차 화면 캡처 목록 (학교 PC에서 찍는다)

이 목록은 **AI로 만들지 않는다.** 학교 PC에서 실제 화면을 찍는다. 찍을 때 넣을 표시는 교과서 HTML의 `<figure>` 바로 위 주석에 그대로 적혀 있다. 여기서는 **한 번에 다 찍기 위한 순서**만 정리한다.

찍는 순서대로 하면 같은 상태를 두 번 만들지 않아도 된다.

| 순서 | 파일 | 언제 찍는가 | 표시할 것 |
|---:|---|---|---|
| 1 | `03-1-02-uv-version-ok` | uv 설치 직후 새 PowerShell | 번호가 나온 줄 |
| 2 | `03-1-03-jupyter-extension` | 확장 화면에서 Jupyter 검색 | **게시자 Microsoft** |
| 3 | `03-1-04-folder-opened` | `03` 폴더를 연 직후 | 탐색기 맨 위 `03` |
| 4 | `03-1-05-uv-sync-before-after` | `uv sync` 전 / 후 탐색기 | 새로 생긴 `.venv` |
| 5 | `03-1-06-kernel-button` | Notebook 열고 아직 안 고른 상태 · **화면 전체** | 오른쪽 위 버튼에 빨간 동그라미 |
| 6 | `03-1-07-kernel-picker` | 5번 화면에서 버튼을 누른 직후 | 목록의 `.venv` 항목 |
| 7 | `03-1-08-kernel-selected` | 고른 직후 · **오른쪽 위만 확대** | 이름 안의 `.venv` |
| 8 | `03-1-09-first-cell-output` | 첫 셀 실행 후 | — (개인 경로는 가린다) |
| 9 | `03-2-05-index-error` | 3-2 범위 초과 셀 실행 후 | 오류 마지막 줄 |
| 10 | `04-1-00-kernel-picker` | 4-1 · `04` 폴더의 Notebook 커널 선택 | 경로에 `04`와 `.venv`가 함께 보이는 항목 · **완료(그림)** |
| 11 | `04-1-03-matplotlib-error` | 4-1 · matplotlib이 없는 상태에서 import 실행 후 | 오류 마지막 줄과 복사 위치 · **완료(그림)** |
| 12 | `04-1-03-graph-in-notebook` | 4-1 · AI가 라이브러리를 추가하고 변경 확인 뒤 그리기 셀 실행 | 코드와 그래프가 한 화면에 · **완료(그림)** |
| 13 | `04-2-01-dataframe-shown` | 4-2 · `df` 셀 실행 후 | 열 이름과 왼쪽 행 번호 0·1·2 · **완료(그림)** |

5·6·7은 **같은 화면에서 세 번 자르는 것**이다. Notebook을 한 번 열고 버튼 누르기 전 → 누른 직후 → 고른 직후를 이어서 찍는다.

**공통 주의**

- 화면 크기는 1600×900 이상. 축소해서 봐도 표시가 보이게 테두리를 굵게.
- 파일 경로에 실명이 나오지 않게 한다. 사용자 폴더 이름이 그대로 찍히기 쉽다.
- 폴더는 **회차 폴더(`03`) 자체를 연 상태**로 찍는다. 상위 폴더를 열면 커널 목록에 `.venv`가 나오지 않는다.

---


## 3-b2. 3주차 ChatGPT 이미지 목록

프롬프트는 교과서 HTML 의 `<figure>` 바로 위 주석에 있다. 그 자리에서 그대로 복사해 쓴다.

| 파일 | 어느 쪽 | 무엇을 보이는가 |
|---|---|---|
| `03-1-11-until-when` | 03-1 · 다르게 읽힌다 | 탁상 달력 두 개에 3 과 2. **2026-09-14 다시 만드는 중** |
| `03-2-01-ruler-zero` | 03-2 · 거리로 읽기 | 자의 시작점은 1이 아니라 0 |
| `03-2-02-list-index` | 03-2 · list 그림 | 값 네 개(72·85·90·68)와 0부터의 위치 번호 |
| `03-2-04-too-many-names` | 03-2 · 왜 묶는가 | 이름표만 늘어나 감당이 안 되는 장면 |
| `03-2-06-dict-labels` | 03-2 · dict 그림 | 서랍 셋에 name·score·passed. score 만 열려 있다 (2026-09-14 라벨 넣어 다시 만듦) |
| `03-2-07-list-vs-dict` | 03-2 · 두 가지 비교 | 왼쪽 사물함 0·1·2·3, 오른쪽 서랍 name·score·passed (2026-09-14 라벨 넣어 다시 만듦) |
| `03-2-08-dicts-to-table` | 03-2 · 여러 명 | 카드 탭 name·club 이 표의 머리줄이 된다 (2026-09-14 라벨 넣어 다시 만듦 · 탭이 파랑·초록이라 브랜드 색에서 벗어나 있다) |

`03-2-02-list-index` 는 옛 파일이 남아 있지만 **값이 본문과 다르다.** 새로 만든다.

### 코드로 만드는 그림 (선생님이 만들지 않는 것)

데이터 그래프만 코드로 만든다. 생성 스크립트는 `04/sample/teacher_tools/` 에 있고, 다시 만들 일이 있으면 그것만 돌리면 된다.

| 파일 | 어느 쪽 | 만드는 스크립트 |
|---|---|---|
| `04-1-01-visitors-bar` | 04-1 · 같은 자료, 두 가지로 | `make_04_charts.py` |
| `04-1-04-axis-ab` · `04-1-05-axis-ab-reveal` | 04-1 · A·B 판정과 공개 | `make_04_axis_comparison.py` |
| `04-1-06-partial-months` · `04-1-07-full-months` | 04-1 · 자른 기간과 전체 | `make_04_axis_comparison.py` |
| `04-2-02-dataframe-bar` | 04-2 · `df.plot` 결과 | `make_04_charts.py` |

**그림 안의 글자는 전부 영문이다.** 학교 PC 에 한글 글꼴이 없어 한글은 네모로 나온다. 한글이 꼭 들어가야 하는 그림은 코드로 만들지 않고 교과서·PPT 에서 표로 그린다.

---

## 3-c. 정리 기록 (2026-09-07)

교과서·PPT 어디에서도 부르지 않는 이미지를 정리했다.

- **삭제 8개** — 같은 그림의 중간 시안·변형본. 최신본이 따로 살아 있어 남길 이유가 없다.
  `02-2-run-button` 4종 · `02-1-06-vscode-python-extension` · `02-1-08-opencode-api-key-input` ·
  `02-1-09-opencode-model-deepseek` · `02-1-vscode-anatomy`
- **`_retired/` 로 옮김 18개** — 설계가 바뀌어 빠졌지만 그림 자체는 완성본이고 SVG 원본이 있다. 되살릴 수 있다.
  `01-2-02-scope` · `01-2-03-io-flow` · `02-1-01-notebook-order` · `02-2-01-vscode-4` · `02-2-02-venv` ·
  `02-2-03-three-pythons` · `02-3-02-first-browser` · `03-1-01-variable-label` · `03-2-01-control-flow` ·
  `04-1-03-same-mean`

### 정리 기록 (2026-09-10)

4주차를 다시 설계하면서 쓰이지 않게 된 그림 7개를 `_retired/` 로 옮겼다. 삭제하지 않았다.
`04-1-01-chart-types`(png·svg) · `04-1-02-axis-trick`(png·svg) · `04-1-04-visitors-bar` · `04-2-01-notebook-flow`(png·svg)

기준: **화면에 안 나오는 것만 건드린다.** 수업에 실제로 쓰인 그림은 남긴다.
같은 그림의 시안이 여러 개면 최신본 하나만 남기고 지운다. 설계 변경으로 빠진 완성본은 지우지 말고 `_retired/` 에 둔다.

---

## 4. 진행 체크리스트

| ID | 생성 | 라벨 | 압축 | 배치 | 인쇄 확인 |
|---|:--:|:--:|:--:|:--:|:--:|
| `00-01-course-map` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `01-1-01-literacy` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `01-2-01-human-computer` | ☑ | — | ☑ | ☑ | ☑ |
| `01-2-04-cpu-ram-storage-analogy` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `01-2-02-cli-gui` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `01-2-03-scope` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `02-1-01-notebook-order` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `02-2-01-vscode-4` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `02-2-02-venv` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `02-2-03-three-pythons` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `03-1-01-result-location` | ☑ | — | ☑ | ☑ | ☑ |
| ~~`03-1-01-variable-label`~~ | — | — | — | — | — |
| `03-2-01-control-flow` | ☑ | — | ☑ | ☑ | ☐ |
| `03-2-02-list-index` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `04-1-01-visitors-bar` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `04-1-00-kernel-picker` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `04-1-02-library-shelf` | ☑ | — | ☑ | ☑ | ☐ |
| `04-1-03-matplotlib-error` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `04-1-03-graph-in-notebook` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `04-1-04-axis-ab` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `04-1-05-axis-ab-reveal` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `04-1-06-partial-months` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `04-1-07-full-months` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `04-2-01-dataframe-shown` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `04-2-02-dataframe-bar` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `04-2-03-hidden-questions` | ☑ | — | ☑ | ☑ | ☐ |
| `05-2-01-submission-layout` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `06-1-01-endless-list` | ☑ | — | ☑ | ☑ | ☐ |
| `07-1-01-points-to` | ☑ | — | ☑ | ☑ | ☐ |
| `09-1-01-no-signpost` | ☑ | — | ☑ | ☑ | ☐ |
| `10-1-01-counter` | ☑ | — | ☑ | ☑ | ☐ |
| `11-1-01-vanished` | ☑ | — | ☑ | ☑ | ☐ |
| `11-1-02-one-file` | ☑ | — | ☑ | ☑ | ☐ |
| `12-1-01-where-do-i-press` | ☑ | — | ☑ | ☑ | ☐ |
| `12-2-01-tangled` | ☑ | — | ☑ | ☑ | ☐ |

**인쇄 확인**은 브라우저에서 `🖨 인쇄`를 눌러 A4 미리보기로 본다. 화면에서 괜찮아도 인쇄에서 흐리거나 잘리는 경우가 있다.

---

## 5. 앞으로 추가할 그림

07-1 이후 차시를 집필하면서 이 문서에 계속 덧붙인다. ID 규칙은 `<차시>-<순번>-<짧은이름>`이다.

집필하며 그림이 필요하다고 판단하는 기준은 다음과 같다.

- 글로 두 문단 이상 설명해야 겨우 전달되는 관계나 구조인가?
- 학생이 **눈으로 본 적 없는 화면**인가? → 캡처
- **시간에 따른 변화나 순서**인가? → 흐름도
- 이미 본문의 `.stack`, `.vflow`, `.flow`, `.iot` 같은 CSS 컴포넌트로 충분히 표현되는가? → **그림을 만들지 않는다**

마지막 기준이 중요하다. 교과서 CSS에 도식용 컴포넌트가 이미 갖춰져 있으므로, 그림은 **CSS로 표현할 수 없는 것**에만 쓴다.

---

## 6. 6~12회 그림 (2026-09-11 갱신)

6~13회는 그림 없이 집필되어 있었지만, 라벨 없이 장면만으로 뜻이 서는 여덟 자리를 골라 등재했다. 2026-09-11에는 `06-1-01`, `09-1-01`, `12-1-01`, `12-2-01` 네 자리가 HTML에 추가되어 제작·배치했다.

고르는 데 쓴 기준 두 가지.

1. **라벨 없이 장면만으로 뜻이 서는가.** 표·화살표·단계 이름이 붙어야 읽히는 도해는 ChatGPT 로 넘기지 않고 CSS 도식으로 둔다. (07-1 §7 1:N, 10-1 §2 요청·응답 흐름, 12-1 §6 처음 달라진 단계가 여기 걸려 제외됐다.)
2. **그 쪽에 자리가 있는가.** A4 인쇄 영역 1002px 안에서 16:9 그림 한 덩이가 약 437px 을 먹는다. 12-1·12-2 도입 쪽은 각각 964px·915px 이라 자리가 없어 제외했다. 자리가 없다고 맥락이 다른 절로 옮기지 않는다.

| ID | 위치 | 종류 | 비율 | 상태 |
|---|---|---|---|---|
| `06-1-01-endless-list` | 06-1 · 들어가며 | ChatGPT · 사실적 삽화 A | 16:9 | **AI 생성 완료** |
| `07-1-01-points-to` | 07-1 · 6. 외래키 (17쪽) | ChatGPT · 사실적 삽화 A | 16:9 | **AI 생성 완료** |
| `09-1-01-no-signpost` | 09-1 · 들어가며 | ChatGPT · 사실적 삽화 A | 16:9 | **AI 생성 완료** |
| `10-1-01-counter` | 10-1 · 1. API는 약속된 창구다 (3쪽) | ChatGPT · 사실적 삽화 A | **21:9** | **AI 생성 완료** |
| `11-1-01-vanished` | 11-1 · 들어가며 (3쪽) | ChatGPT · 사실적 삽화 A | 16:9 | **AI 생성 완료** |
| `11-1-02-one-file` | 11-1 · 3. SQLite (8쪽) | ChatGPT · 사실적 삽화 A | 16:9 | **AI 생성 완료** |
| `12-1-01-where-do-i-press` | 12-1 · 들어가며 | ChatGPT · 사실적 삽화 A | 16:9 | **AI 생성 완료** |
| `12-2-01-tangled` | 12-2 · 들어가며 | ChatGPT · 사실적 삽화 A | 16:9 | **AI 생성 완료** |

여덟 개 모두 **공통 스타일 A(사실적 삽화)** 를 앞에 붙이고, 끝에 `no text, no letters, no numbers, no labels, no watermark` 를 붙인다. B(플랫 벡터)를 같이 붙이지 않는다.

각 항목의 한국어 맥락·그릴 것·지킬 것은 **교과서 HTML 의 `<figure>` 바로 위 주석**에 그대로 들어 있다. 아래에는 영어 프롬프트 본문만 둔다. 둘 중 하나를 고치면 다른 쪽도 같이 고친다.

---

### 06-1-01-endless-list ★★★

- **위치** — `06-1.html` · 들어가며 · PPT 06-1 02쪽
- **목적** — 기능 목록은 길지만 무엇이 완성인지 정해지지 않은 상태를 끝없는 종이 목록으로 보여 준다.
- **비율·상태** — 16:9 · 1600×900 · 678KB · AI 생성·압축·배치 완료
- **받은 뒤 확인** — 목록이 종이와 책상 아래로 계속 이어지고, 모든 체크 칸이 비어 있으며, 읽히는 글자가 없고, 첫 체크 칸 테두리만 주황인가.

```
A quiet bright desk seen from a gentle overhead angle. One sheet of paper holds
a dense list made only from abstract horizontal strokes and empty square
checkboxes. The list continues beyond the bottom edge of the paper, hangs over
the front edge of the desk, and leaves the frame with no visible ending.
Every checkbox is empty. Signal orange #FF5C00 appears only on the thin outline
of the first checkbox. One relaxed hand may rest near the top; no face.
Warm realistic editorial illustration, soft natural light, muted neutrals,
near-white background, generous empty space. Aspect ratio: 16:9.
no text, no letters, no numbers, no labels, no watermark
```

---

### 07-1-01-points-to ★★★

- **위치** — `07-1.html` · 6. 외래키는 다른 표의 행을 가리킨다 (이어지는 쪽, 17쪽) · PPT 07-1 10쪽
- **캡션** — 그림 7-1-1. 왼쪽 표의 한 칸이 오른쪽 표의 한 줄을 가리킨다. 외래키가 하는 일이 이것이다.
- **목적** — 외래키 절은 지금 용어 상자와 코드만 있어 **두 표 사이에 무슨 일이 일어나는지가 안 보인다.** 「가리킨다」를 한 장으로 잡는다.
- **비율** — 16:9 · 쪽 여유 554px (그림 덩이 437px)
- **판단** — 라벨이 필요 없다. 실 한 가닥이 어디서 어디로 가는지만 보이면 된다. 그래서 SVG 가 아니라 ② 갈래로 넘긴다.
- **받은 뒤 확인** — ① 실이 한 가닥인가 ② 실이 오른쪽 표의 **한 줄**에 닿았나(표 전체·제목이 아니라) ③ 표의 글자가 읽히지 않나 ④ 주황이 실에만 있나

```
A quiet desk scene, warm realistic editorial illustration.

Two sheets of printed paper lie side by side on a plain desk. Both sheets show
a small grid — ruled rows and columns — printed in light grey. The printing is
deliberately too small to read: only the rules and the cells are legible, never
any word or character.

From ONE single cell on the left sheet, a fine thread rises off the paper,
crosses the gap between the sheets, and comes down to touch ONE single row of
the right sheet. Exactly one thread. It ends on that one row, not on the whole
sheet and not on the sheet's heading.

The thread is signal orange #FF5C00. It is the only orange in the picture;
everything else is muted neutral and warm grey. Soft natural daylight from the
side, gentle paper shadows. A hand may rest at the edge of the desk; no face.

Plain near-white background #ffffff, generous empty space, uncluttered.
NOT a flat vector diagram, NOT boxes-and-arrows, NOT an infographic,
no gradients, no drop shadows, no 3D render, no emoji, no dark background.
Aspect ratio: 16:9

no text, no letters, no numbers, no labels, no watermark
```

---

### 09-1-01-no-signpost ★★★

- **위치** — `09-1.html` · 들어가며 · PPT 09-1 02쪽
- **목적** — 기능이 없는 것이 아니라 처음 온 사람이 지나갈 길을 찾을 수 없는 상태를 보여 준다.
- **비율·상태** — 16:9 · 1600×900 · 690KB · AI 생성·압축·배치 완료
- **받은 뒤 확인** — 안내판·번호표·유도선·글자가 없고, 창구는 여러 개이며, 사람이 뒷모습 한 명뿐이고, 주황이 가방 한 점뿐인가.

```
A bright, quiet and empty public-service lobby viewed from the rear. Five plain
service-counter openings stand side by side along the far wall. Exactly one
person stands in the middle, seen only from behind, uncertain where to go.
There is no information desk, signboard, counter number, notice, ticket machine,
arrow, floor guide line, screen or directional cue. The walls remain blank.
Signal orange #FF5C00 appears only on one tiny part of the person's shoulder bag.
Warm realistic editorial illustration, soft daylight, muted neutrals,
near-white background, calm rather than lonely. Aspect ratio: 16:9.
no text, no letters, no numbers, no labels, no watermark
```

---

### 10-1-01-counter ★★★

- **위치** — `10-1.html` · 1. API는 약속된 창구다 (3쪽) · PPT 에는 넣지 않는다 — 21:9 는 슬라이드에서 너무 납작해진다
- **캡션** — 그림 10-1-1. 정해진 창구에 정해진 것을 내면, 정해진 것이 돌아온다.
- **목적** — API 라는 말을 처음 듣는 자리다. 교과서가 쓰는 「약속된 창구」 비유를 장면으로 먼저 보여 준다.
- **비율** — **21:9** · 쪽 여유 456px. **16:9 로 만들면 그 쪽이 983px 이 되어 인쇄에서 위태롭다.** 창구를 옆에서 본 장면이라 납작한 비율이 내용과도 맞는다.
- **판단** — 비유를 그리는 그림이라 모니터·아이콘·코드가 들어가면 안 된다. 「그리지 말 것」을 주석에 같이 적었다.
- **받은 뒤 확인** — ① 창구가 **하나**인가(은행 로비처럼 여러 개면 다시) ② 주고받는 종이가 각각 한 장인가 ③ 글자가 없나 ④ 화면·기기가 끼어들지 않았나

```
A small service counter seen from the side, warm realistic editorial
illustration.

A single counter with a glass screen above it and a narrow slot underneath,
just wide enough to pass paper through. From one side, a hand pushes one sheet
of paper into the slot. From the other side, another hand is handing one sheet
of paper back out. Only hands are visible; no faces.

Exactly ONE counter. Do not draw a bank lobby with a row of windows — the whole
point is that there is one agreed place to go.

The paper carries no readable marks. Signal orange #FF5C00 appears only as a
small accent around the slot itself; everything else is muted neutral and warm
grey. Soft natural light, calm and quiet.

No computers, no monitors, no screens, no icons, no keyboards anywhere in the
picture. This is a metaphor, not a user interface.

Plain near-white background #ffffff, generous empty space.
NOT a flat vector diagram, NOT an infographic, no gradients, no drop shadows,
no 3D render, no emoji, no dark background.
Aspect ratio: 21:9 (wide and short)

no text, no letters, no numbers, no labels, no watermark
```

---

### 11-1-01-vanished ★★★

- **위치** — `11-1.html` · 들어가며 · 새로 열었더니 사라졌다 (이어지는 쪽, 3쪽) · PPT 11-1 02쪽
- **캡션** — 그림 11-1-1. 방금 남긴 것이 다시 왔을 때 없다. 11회는 여기에서 시작한다.
- **목적** — 11회 전체의 출발점. 10회에 화면까지 이어 놓았는데 창을 새로 열자 사라졌다. 설명 문장 앞에 장면을 먼저 둔다.
- **비율** — 16:9 · 쪽 여유 684px
- **판단** — 03-1 `result-location` 과 같은 「차시 도입 장면」 자리다. 감정을 과장하지 않는다. 조용한 장면이어야 다음 쪽 설명이 산다.
- **받은 뒤 확인** — ① 글자로 읽히는 것이 없나 ② 사라져 가는 중인 것이 보이나(이미 다 지워졌으면 뜻이 다르다) ③ 주황이 한 군데뿐인가 ④ 인쇄해도 흐릿하지 않나

```
A close view of a fogged window pane, warm realistic editorial illustration.

Someone has drawn something on the misted glass with a fingertip. The marks are
pure strokes — sweeping, handwritten gestures that belong to no alphabet and
cannot be read as any letter or character. In the upper half the strokes are
still there. Lower down, condensation has run and the strokes have dissolved
into streaks and drips, half gone.

Beyond the glass, the outside is only a soft blur of daylight.

One short surviving stroke carries a trace of signal orange #FF5C00; every other
mark is muted neutral grey. Bright, calm and quiet — not sad, not dramatic.
At most a single fingertip may appear at the edge; no face, no person.

Light near-white overall tone, uncluttered, generous empty space.
NOT a flat vector diagram, NOT an infographic, no gradients on flat panels,
no drop shadows, no 3D render, no emoji, no dark background.
Aspect ratio: 16:9

no text, no letters, no numbers, no labels, no watermark
```

---

### 11-1-02-one-file ★★★

- **위치** — `11-1.html` · 3. SQLite는 프로젝트가 기억할 때 쓰는 파일이다 (이어지는 쪽, 8쪽) · PPT 11-1 05쪽
- **캡션** — 그림 11-1-2. 따로 돌아가는 장치가 아니라, 폴더 안의 파일 하나다.
- **목적** — 학생이 데이터베이스를 「어딘가 따로 돌아가는 큰 것」으로 오해한다. 실제로는 프로젝트 폴더 안 파일 하나다.
- **비율** — 16:9 · 쪽 여유 554px
- **판단** — 요점이 **개수**에 걸려 있다. 상자를 두 개 이상 그리면 그림이 본문과 반대말을 한다. 주석에 「상자는 하나」를 못 박았다.
- **받은 뒤 확인** — ① 상자가 **하나**인가 ② 서버 랙·기계·전선이 끼어들지 않았나 ③ 카드에 글자가 없나 ④ 주황이 한 군데뿐인가

```
A single small box on a plain desk, warm realistic editorial illustration.

The box has its lid open. Inside, thin cards stand upright in a neat row,
packed close together, filling the box. The cards are blank — no marks, no
writing, nothing readable. Only their edges and the way they line up is visible.

There is exactly ONE box, and the space around it is empty. Do not draw a second
box, a server rack, a machine, cables, plugs, or any electronic equipment
anywhere in the picture — the point of this image is that there is no separate
machine, only this one container.

The top edge of one card carries a small touch of signal orange #FF5C00; nothing
else in the picture is orange. Everything else is muted neutral and warm grey.
Soft natural daylight, gentle shadow under the box. A hand may rest beside it;
no face.

Plain near-white background #ffffff, generous empty space, uncluttered.
NOT a flat vector diagram, NOT an infographic, no gradients, no drop shadows,
no 3D render, no emoji, no dark background.
Aspect ratio: 16:9

no text, no letters, no numbers, no labels, no watermark
```

---

### 12-1-01-where-do-i-press ★★★

- **위치** — `12-1.html` · 들어가며 · PPT 12-1 02쪽
- **목적** — 만든 사람은 알고 있지만 처음 온 사용자는 무엇을 눌러야 할지 몰라 멈추는 순간을 보여 준다.
- **비율·상태** — 16:9 · 1600×900 · 582KB · AI 생성·압축·배치 완료
- **받은 뒤 확인** — 화면 안이 완전히 비었고, 손 하나가 자판에 닿지 않은 채 멈춰 있으며, 얼굴이 없고, 주황이 화면 아래 한 점뿐인가.

```
A bright quiet desk with exactly one open laptop, viewed from a close
three-quarter side angle. The display is a completely blank field of white
light with no interface, button, icon, cursor or shape. Exactly one hand and
forearm hover motionless several centimeters above the keyboard without
touching it. No face or full person. Signal orange #FF5C00 appears only as one
tiny glint from the lower corner of the otherwise white screen.
Warm realistic editorial illustration, soft daylight, muted neutrals,
near-white background, no clutter. Aspect ratio: 16:9.
no text, no letters, no numbers, no labels, no watermark
```

---

### 12-2-01-tangled ★★★

- **위치** — `12-2.html` · 들어가며 · PPT 12-2 02쪽
- **목적** — 시험하면서 여러 가지를 계속 고쳐 원인을 추적할 수 없게 된 상태를 엉킨 실로 보여 준다.
- **비율·상태** — 16:9 · 1600×900 · 698KB · AI 생성·압축·배치 완료
- **받은 뒤 확인** — 대여섯 가닥이 추적 불가능하게 엉켜 있고, 주황 실은 한 가닥뿐이며 그 실도 매듭에 묻혔고, 푸는 사람이나 도구가 없는가.

```
A quiet near-white tabletop with one compact tangled mass of six thin cords.
Five cords are muted gray or beige. Exactly one cord is signal orange #FF5C00.
Every cord loops over and under the others so no strand can be followed from
beginning to end. The orange cord appears only in short buried fragments inside
the tangle and neither endpoint is traceable. No hands, people, scissors,
spools, tools or untangling action.
Warm realistic editorial illustration, soft daylight, muted neutrals,
generous empty space. Aspect ratio: 16:9.
no text, no letters, no numbers, no labels, no watermark
```

---

### 03-1-12-different-work-different-tools ★★★

- **위치** — `03-1.html` · 3. 도구 준비 · 그림 (4쪽) · PPT 03-1 05쪽
- **캡션** — 그림 3-1-2. 하는 일이 다르면 챙기는 도구가 다르다. 폴더도 마찬가지다.
- **목적** — 「폴더마다 필요한 것이 다르다」를 말이 아니라 한 장으로 굳힌다. uv 가 왜 필요한지의 근거가 되는 비유다.
- **비율·상태** — 16:9 · 1600×900 · 793KB · AI 생성·배치 완료
- **받은 뒤 확인** — 하는 일이 **동작과 작업대로** 갈리는가, 양쪽 도구에 겹치는 물건이 하나도 없는가, 얼굴·끼어든 손이 없는가, 한쪽이 더 커 보이지 않는가, 주황이 한 군데뿐인가.

> [!note] 앞 그림을 왜 바꿨나 (2026-09-14)
> 원래 `03-2-03-project-toolboxes` 를 재사용했는데, 두 사람이 **둘 다 책상에서 공부를 하고 있어** 「하는 일이 다르면」이 그림에 없었다. 다른 것은 도구함 속 아이콘뿐이라 캡션만 혼자 떠 있었다. 가운데에 정체불명의 손도 하나 있었다.
> 학교 교재라 「공부 vs 막일」 구도는 위아래로 읽혀서 피하고, 서로 다른 두 **일**(제빵 / 자전거 정비)로 잡았다. 3주차의 다른 그림들이 실사 편집사진이라 톤도 거기에 맞췄다.

```
Two separate work counters side by side in one wide frame, divided by a narrow
gap of empty background down the middle. Two different trades, mid-work.

LEFT counter — bread baking. A floured wooden surface with a round of dough
resting on it. Laid out beside it: a metal mixing bowl, a kitchen scale, a
wooden rolling pin, a dough scraper, a balloon whisk. A pair of hands and
forearms, sleeves rolled, working the dough.

RIGHT counter — bicycle repair. A bicycle wheel held upright in a repair
stand. Laid out beside it: a set of open-end wrenches, two tire levers, a
chain tool, a small oil bottle, a rag. A different pair of hands and forearms
turning a wrench on the hub.

The two tool sets share no single item. Nothing on the left counter appears
on the right, and nothing on the right appears on the left.

No faces, no heads — hands and forearms only, cropped at the elbow. Neither
side is larger, brighter or more central than the other; both counters get
equal width and equal light. Exactly one small object in the frame is signal
orange #FF5C00 (a single tool handle); everything else stays in muted
neutrals — wood, flour white, warm grey, steel.

Warm realistic editorial photograph, soft daylight from the side, shallow
depth of field, plain off-white background above the counters, generous
empty space. Aspect ratio: 16:9. Output 1600x900 PNG.

no text, no letters, no numbers, no labels, no signage, no logos, no watermark
```

---

### 등재하지 않은 자리와 그 까닭

| 자리 | 왜 안 만드나 |
|---|---|
| 03-1 경계선 보기 (`03-1-10-boundary-number-line`) | 2026-09-14 절을 통째로 뺐다. 바로 앞 쪽이 `>` 와 `>=` 를 코드와 실행 결과로 이미 보여 주고, 덱에도 같은 수직선 장이 있었다. 같은 말이 세 번이었다. 파일은 `assets/img/_retired/` 로 옮겼다. |
| 03-2-03-project-toolboxes (기존 자산) | 3-1 4쪽에 쓰다가 2026-09-14에 뺐다. 두 사람이 둘 다 공부하는 그림이라 「하는 일이 다르다」가 안 보였다. 파일은 남겨 두되 어디에도 걸려 있지 않다. |
| 07-1 §7 1:N 관계 | 「하나 ↔ 여럿」은 라벨이 붙어야 읽힌다. 본문 표와 `.steps` 로 충분하다. |
| 07-1 §8 JOIN | 이미 좌우 패널 두 개로 대조를 보여 주고 있다. 그림이 겹친다. |
| 10-1 §2 요청·응답 흐름 | 단계 이름이 전부 라벨이다. CSS 도식이 낫다 — 한중 병기가 되고 나중에 글자만 고칠 수 있다. |
| 11-2 §6 확인이 세 번 | 세 번의 내용이 전부 글자다. |
| 12-1 §6 처음 달라진 단계 | 단계 이름이 라벨이다. 지금 `code` 블록으로 순서를 보여 주고 있다. |
