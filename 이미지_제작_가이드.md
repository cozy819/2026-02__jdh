---
title: 교과서 이미지 제작 가이드
tags: [장대현중고등학교, 정보교육, 교과서, 이미지]
status: 작성중
created: 2026-08-12
updated: 2026-08-24
---

# 교과서 이미지 제작 가이드

상위 문서: [[00 정보와 디지털 문해력 - 수업구성]] · [[08 교과서 집필 기준]]

`교과서_배포/`의 HTML에 들어갈 그림의 **위치, 목적, 생성 프롬프트**를 모아 둔 문서다. 그림을 만들 때마다 이 문서를 갱신한다.

---

## 0. 담당과 작업 경계

- Claude는 교과서 원고와 HTML 집필을 담당한다.
- Codex는 교과서 이미지 작업을 담당한다.
  - 본문과 HTML을 확인해 이미지가 실제로 필요한지 판단한다.
  - AI 생성 이미지의 프롬프트를 설계하고 이미지를 생성한다.
  - 라벨이 많은 개념도는 SVG로 직접 제작한다.
  - 결과물을 지정된 파일명으로 `assets/img/`에 배치한다.
  - 파일 크기, 화면 표시와 A4 인쇄 결과를 확인한다.
  - 생성·수정·배치가 끝날 때마다 이 가이드의 진행 체크리스트를 갱신한다.
- 실제 프로그램 화면의 정확성이 중요한 화면 캡처는 생성 이미지로 대체하지 않는다. 교실과 같은 환경에서 캡처한 원본을 사용하고, Codex는 번호·테두리·개인정보 가림과 최종 배치를 담당한다.
- 원고나 HTML의 내용이 변경되면 기존 이미지 목록과 목적도 다시 검토한다. 자리표시자가 있다는 이유만으로 이미지를 그대로 만들지 않는다.

### 제작 순서

1. Claude가 해당 차시의 HTML 초안을 확정한다.
2. Codex가 본문, 자리표시자와 이미지 가이드를 대조한다.
3. CSS·SVG·화면 캡처·AI 생성 중 적절한 방식을 선택한다.
4. AI 생성이 필요한 이미지는 라벨 없이 만들고 시각적 정확성을 검토한다.
5. 필요한 라벨을 별도 방식으로 얹고 PNG로 내보내거나 SVG로 완성한다.
6. 지정된 ID로 저장하고 스크롤·슬라이드·인쇄 모드에서 확인한다.

현재 HTML이 참조하는 이미지 **17개가 모두 완성되어** `assets/img/`에 배치되어 있다. 각 도식은 수정 가능한 SVG 원본과 HTML 배포용 PNG를 함께 둔다. (`01-2-02-cli-gui`는 2026-08-19 설계 개편으로 폐기되어 `_retired/`로 옮겼다.)

> [!important] 6~8회에는 이미지를 만들지 않는다
> 2026-08-19에 6-1~8-2 여섯 차시를 집필했으나 **자리표시자를 하나도 넣지 않았다.** 설계 원고(6-1 §7, 6-2 §6, 8-1·8-2)가 “화면은 그림 대신 입력·동작·결과를 설명하는 글로 작성한다”를 반복해 못 박고 있고, 이 범위의 개념(사용자·기능 범위·표와 관계)은 `.vflow`·`.iot`·`.kv`·`.stack` 같은 CSS 도식으로 충분히 표현된다.
> 이 차시들에 그림을 추가할 때는 **본문에서 도식을 대체하지 말고**, 새 `figure.fig`를 넣을 자리를 먼저 이 문서에 등재한 뒤 제작한다.

> [!warning] 2026-08-19 개편으로 차시 번호가 바뀌었다
> 옛 4-1(흐름·오류)이 **3-2**로, 옛 5-1·5-2가 **4-1·4-2**로, 옛 6-1·6-2가 **5-1·5-2**로 내려왔다. 이미 완성된 10개(`00-01` ~ `03-1-01`)는 ID가 그대로이므로 **다시 만들 필요가 없다.** 다만 `00-01-course-map`은 학기 구조가 1주 / 2~5주 / 6~8주 / 9~13주 네 단계로 바뀌어 **SVG와 PNG를 갱신했다.**

---

## 1. 작업 방식

### 파일을 넣기만 하면 된다

HTML은 이미 `assets/img/<ID>.png`를 참조하도록 작성되어 있다. **HTML을 고칠 필요가 없다.**

```
교과서_배포/assets/img/01-2-01-human-computer.png   ← 이 이름으로 저장하면 끝
```

파일이 없으면 `textbook.js`의 `imgPlaceholder()`가 자동으로 안내 상자를 보여 준다. 지금 브라우저로 열어 보면 점선 상자에 `IMG 01-2-01-human-computer`와 설명이 표시된다. 파일을 넣고 새로고침하면 그 자리에 그림이 들어간다.

### 브랜드

2026-08-19부터 교과서는 **바이브코딩연구소 2026 브랜드**를 따른다. 새로 만드는 그림도 같은 규격에 맞춘다. (원본: `2608-02_harness_v2/templates/brand/문서-스타일-스펙.md`)

| 항목 | 값 |
|---|---|
| 강조 | 시그널 오렌지 `#FF5C00` — 화면의 5% 이하 |
| 본문·선 | 카본 `#1B1D21` |
| 음영 | 쿨 그레이 `#F1F2F4` — 다른 색으로 면을 채우지 않는다 |
| 보조 텍스트 | 스틸 `#6C7078` |
| 긍정 상태 | 틸 `#0E7C86` |
| 글꼴 | 국문 IBM Plex Sans KR · 영문/수치 Archivo · 코드 IBM Plex Mono |

- **그라데이션과 그림문자(이모지)를 넣지 않는다.**
- 모서리는 각지게 둔다(둥근 모서리 없음).
- 구조를 이루는 선과 테두리는 **회색**(`#6C7078` · `#C7C9CE`)으로 둔다. 선까지 주황으로 칠하면 강조가 사라진다.
- 하지 말 것을 표시할 때만 `#C24A00`을 쓴다. 브랜드에 빨강·노랑은 없다.
- **화살촉은 크기를 고정한다.** `<marker>`에 `markerUnits="userSpaceOnUse"`를 넣고 `markerWidth`/`markerHeight`를 24 안팎으로 둔다. 기본값이면 선이 굵어질수록 화살촉이 같이 커져 도형을 덮는다. 또 `marker-end`는 경로의 **마지막 꼭짓점에만** 붙으므로, 갈래마다 화살촉이 필요하면 `<path>`를 나눈다.
- 2026-08-24에 인디고 팔레트로 남아 있던 6개를 **모두 VCL 팔레트로 다시 칠했다.** 이제 그림 전체가 브랜드와 맞는다. (`01-2-01-human-computer`와 `02-2-01-vscode-4`는 SVG 원본이 없어 예외로 둔다.)

> [!warning] 문장 하나를 그린 그림은 만들지 않는다
> `01-1-01-literacy`(동심원 4단)를 2026-08-24에 폐기했다. 동심원이 주장하는 것은 **“A가 B를 포함한다”** 한 문장뿐이어서, 그 문장을 글로 쓰면 그림이 더할 것이 없었다. 게다가 **대상(정보)과 능력(문해력)을 한 축에 섞어** 범주가 어긋났고 포함 순서도 뒤집혀 있었다.
> 그림은 **글로 하기 어려운 것**(구조·흐름·비교·수치)에만 쓴다. 낱말의 상하 관계는 용어 상자의 `경계` 줄이나 README §5의 CSS 도식(`.stack`)으로 쓴다. **CSS 도식은 한중 병기가 되고 이미지처럼 번역이 막히지 않는다.**

### 형식

- **PNG** 권장. 사진풍 일러스트는 JPG도 무방하다.
- 가로 폭 **1600px 이상**. 인쇄(A4)에서 흐려지지 않을 정도.
- 파일 하나가 **1MB를 넘지 않도록** 압축한다. 교실 노트북이 저사양이고 한 쪽에 여러 이미지가 들어갈 수 있다.
- 배경은 **흰색 또는 아주 옅은 회색**. 교과서 페이지가 흰 바탕이다.

---

## 2. 공통 스타일 (모든 프롬프트 앞에 붙일 것)

교과서 전체가 한 벌로 보여야 하므로 색과 톤을 고정한다.

```
Style: clean modern educational textbook diagram, flat vector style,
generous white space, square corners, thin consistent line weight.
Color palette — signal orange #FF5C00 only for the key point,
carbon #1B1D21 for outlines, cool gray #F1F2F4 for panels,
steel #6C7078 for secondary elements, teal #0E7C86 for positive states,
white background #ffffff.
No gradients, no drop shadows, no 3D, no photorealism, no emoji,
no dark background, no clutter. Calm, precise and legible.
Aspect ratio: <각 항목의 비율>
```

### ⚠️ 텍스트는 이미지에 넣지 않는다

생성형 이미지 모델은 **한글을 거의 정확히 그리지 못한다.** 글자가 깨진 그림은 교과서에 쓸 수 없다.

그래서 각 항목에 **라벨** 칸을 따로 두었다. 처리 방법은 둘 중 하나다.

| 방법 | 언제 | 어떻게 |
|---|---|---|
| **A. 라벨 없이 생성 후 얹기** | 사람·장면 일러스트 | 프롬프트에 `no text, no letters, no labels`를 넣어 그림만 만들고, Figma·Keynote·PowerPoint에서 한글 라벨을 얹어 PNG로 내보낸다. |
| **B. 그림 대신 SVG/CSS** | 도식·개념도 | 아래 표에서 **SVG 권장**으로 표시한 항목. 라벨이 많은 도식은 이미지보다 인라인 SVG가 낫다. 확대해도 깨지지 않고, 인쇄가 선명하고, 나중에 글자만 고칠 수 있다. |

모든 프롬프트 끝에 다음을 붙인다.

```
no text, no letters, no numbers, no labels, no watermark
```

---

## 3. 이미지 목록

전체 17개. **모두 제작·배치 완료**했다. 2026-08-24에는 확정된 HTML 본문과 대체 텍스트를 다시 읽고, 비어 있던 03-2~05-2 도식 7개를 바이브코딩연구소 2026 브랜드로 제작했다. 정확한 값·흐름·순서가 중요한 항목이므로 생성형 이미지 대신 SVG로 설계하고 같은 비율의 PNG로 렌더링했다.

| ID | 위치 | 종류 | 우선순위 |
|---|---|---|---|
| `00-01-course-map` | index.html · 학습 지도 | SVG 완료 | ★★☆ |
| `01-1-01-literacy` | 01-1.html · 네 낱말의 뜻 | SVG 완료 | ★★☆ |
| `01-2-01-human-computer` | 01-2.html · computer는 사람이었다 | **AI 생성 완료** | ★★★ |
| `01-2-02-scope` | 01-2.html · 코딩·프로그래밍·개발 | SVG 완료 | ★☆☆ |
| `02-1-01-notebook-order` | 02-1.html · Notebook의 함정 | SVG 완료 | ★★★ |
| `02-2-01-vscode-4` | 02-2.html · VS Code 네 곳 | **화면 캡처 완료** | ★★★ |
| `02-2-02-venv` | 02-2.html · 가상환경 | SVG 완료 | ★★☆ |
| `02-2-03-three-pythons` | 02-2.html · 인터프리터와 커널 | SVG 완료 | ★★★ |
| `03-1-01-variable-label` | 03-1.html · 변수는 값에 붙인 이름 | SVG 완료 | ★★★ |
| `03-2-01-control-flow` | 03-2.html · 실행 흐름 | SVG 완료 | ★★★ |
| `03-2-02-list-index` | 03-2.html · 위치 번호는 0부터 | SVG 권장 | ★★☆ |
| `04-1-01-chart-types` | 04-1.html · 그래프 종류 고르기 | SVG 완료 | ★★☆ |
| `04-1-02-axis-trick` | 04-1.html · 같은 데이터 다른 인상 | SVG 완료 | ★★★ |
| `04-1-03-same-mean` | 04-1.html · 평균이 같아도 | SVG 권장 | ★★★ |
| `04-2-01-notebook-flow` | 04-2.html · 완성 결과 | SVG 권장 | ★★☆ |
| `05-2-01-submission-layout` | 05-2.html · 제출물 구성 | SVG 권장 | ★★★ |

---

### 00-01-course-map

- **위치** — `index.html` · 학습 지도 쪽
- **캡션** — 그림 0-1. 한 학기 학습 흐름
- **목적** — 왜 창작을 바로 하지 않고 Python부터 배우는지, 그리고 개발 전에 왜 계획하는 구간이 따로 있는지를 한눈에 보여 준다.
- **비율** — 16 : 6 (가로로 긴 띠)
- **판단** — **SVG 확정.** 회차 번호와 구간 이름이 모두 글자다. `00-01-course-map.svg`를 고치고 다시 렌더링한다.
- **상태** — 2026-08-19 갱신 완료. 학기가 네 단계로 바뀌면서 SVG와 PNG를 모두 다시 만들었다.

**라벨 (현재 SVG에 들어 있는 값)**

```
[1주] 오리엔테이션 — "수업 열기"
[2~5주] Python과 데이터 — "읽는 힘"
[6~8주] 기획과 데이터 설계 — "계획하는 힘"
[9~13주] AI와 서비스 개발 — "만드는 힘"
마일스톤 ⑤ 수행평가 1 · 데이터 시각화
마일스톤 ⑧ 수행평가 2 · 제작 계획서
마일스톤 ⑬ 수행평가 3 · 창작물
```

**고칠 때**

```bash
# SVG 를 수정한 뒤 같은 크기로 다시 렌더링한다
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless --disable-gpu --window-size=1600,600 \
  --default-background-color=FFFFFFFF --hide-scrollbars \
  --screenshot="assets/img/00-01-course-map.png" \
  "file://$PWD/assets/img/00-01-course-map.svg"
```

---

### 01-1-01-literacy

- **위치** — `01-1.html` · 2. 낱말 정의 (이어서)
- **캡션** — 그림 1-1-1. 네 낱말은 서로를 포함하며 넓어진다
- **목적** — 정보·문해력·디지털 문해력·AI 문해력이 별개가 아니라 포개진 개념임을 시각화한다.
- **비율** — 16 : 9
- **판단** — **SVG 권장.** 네 개의 낱말이 모두 라벨이다.

**라벨**

```
바깥 → 안 순서
AI 문해력  /  디지털 문해력  /  문해력  /  정보
가장 안쪽 작은 점: 데이터
```

**프롬프트 (이미지로 만들 경우)**

```
Four concentric rounded rectangles nested inside one another, viewed flat from
the front, with comfortable padding between each layer. The innermost shape is a
small solid dot. Each outer layer is progressively lighter in tone.
Flat vector diagram, indigo #4f46e5 outermost outline, layers filled with
#eef2ff, #f4f4f5, white. Thin uniform strokes. White background, generous margin.
no text, no letters, no numbers, no labels, no watermark
```

---

### 01-2-01-human-computer ★★★

- **위치** — `01-2.html` · 1. computer는 사람의 직업이었다
- **캡션** — 그림 1-2-1. 계산을 직업으로 삼았던 사람들, ‘human computer’
- **목적** — “컴퓨터가 원래 사람이었다”는 이 차시의 도입 장면을 시각적으로 각인시킨다. 라벨이 필요 없어 **AI 생성에 가장 적합**하다.
- **비율** — 16 : 9
- **주의** — 특정 실존 인물을 묘사하지 않는다. 시대 분위기만 전달한다. 여성이 다수 포함되도록 한다(본문에서 여성들의 참여를 언급한다).

**프롬프트**

```
A mid-20th-century computing room seen from a gentle three-quarter angle.
Several people, mostly women, sit along long shared desks arranged in rows.
Each person works with paper calculation sheets, ruled tables and a small
mechanical desk calculator. One person passes a sheet to the person beside them,
suggesting that results are checked by someone else. Large windows on one side,
soft daylight. Warm, dignified, focused atmosphere — this is skilled professional work.
Clean modern editorial illustration, flat vector style with subtle texture,
limited palette: indigo #4f46e5 as the dominant accent, warm neutral grays,
cream paper tones, white background. Thin consistent line weight.
No computers, no monitors, no screens of any kind.
no text, no letters, no numbers, no labels, no watermark
```

---

### ~~01-2-02-cli-gui~~ — 폐기 (2026-08-19)

- **상태** — **사용하지 않는다.** 2026-08-19 설계 개편으로 1-2에서 CLI→GUI 절이 삭제되었다. 파일은 `assets/img/_retired/`로 옮겨 두었으며, 되살릴 일이 없으면 지워도 된다.
- **옛 위치** — `01-2.html` · 5. 인터페이스의 역사
- **캡션** — 그림 1-2-2. 같은 일을 하는 두 가지 방식 — CLI와 GUI
- **목적** — 두 인터페이스가 어떻게 생겼는지 본 적 없는 학생에게 실물 감각을 준다.
- **비율** — 16 : 9 (좌우 2분할)
- **주의** — 왼쪽 CLI 화면에 글자가 들어가야 자연스럽다. **의미 없는 문자 나열이라도 무방하므로**, 예외적으로 텍스트 금지 지시를 완화한다. 다만 한글은 넣지 않는다. 깨진 글자가 신경 쓰이면 방법 A(라벨 얹기)로 처리한다.

**프롬프트**

```
A side-by-side comparison of two computer screens on a plain white background,
separated by a thin vertical divider line.

Left screen: a terminal window with a dark navy background (#0e1524),
filled with several short lines of monospaced light-gray Latin text and a
blinking block cursor. No icons, no images, no windows — only text lines.

Right screen: a light desktop interface with two overlapping rounded windows,
a row of simple flat icons along the bottom, a small folder icon and a mouse
pointer arrow. Soft indigo #4f46e5 accents on window title bars.

Both screens drawn as simple flat rounded rectangles with thin #e4e4e7 borders,
same size, aligned. Clean modern flat vector illustration, no shadows, no 3D.
No Korean characters. No watermark.
```

---

### 01-2-02-scope

- **위치** — `01-2.html` · 5. 코딩·프로그래밍·개발
- **캡션** — 그림 1-2-2. 세 낱말이 가리키는 범위
- **목적** — 세 낱말의 포함 관계를 보여 준다.
- **비율** — 1 : 1 (정사각)
- **판단** — **SVG 권장.** 본문에 이미 같은 내용의 `.stack` 도식이 있으므로 **우선순위가 낮다.** 생략해도 무방하다.

**라벨**

```
바깥 → 안 순서
소프트웨어 개발  /  프로그래밍  /  코딩
```

**프롬프트 (이미지로 만들 경우)**

```
Three concentric circles nested inside one another, drawn flat from the front,
each with generous space between rings. Innermost circle filled solid indigo
#4f46e5, middle ring light indigo #eef2ff, outer ring white with a thin
#e4e4e7 outline. Perfectly centered, white background, wide margins.
no text, no letters, no numbers, no labels, no watermark
```

---

### 02-1-01-notebook-order ★★★

- **위치** — `02-1.html` · 4. Notebook의 함정 — 실행 순서
- **캡션** — 그림 2-1-1. 보이는 순서와 실행한 순서는 다를 수 있다
- **목적** — 이 차시에서 **가장 설명하기 어려운 개념**이다. 문서에 보이는 순서와 실제 실행 순서가 어긋난다는 것을 글로만 설명하면 잘 와닿지 않는다. 그림이 꼭 필요하다.
- **비율** — 16 : 9

**라벨** (방법 A로 얹을 것)

```
왼쪽 제목: 문서에 보이는 순서
오른쪽 제목: 실제로 실행한 순서
각 셀 옆 번호: [1] [2] [3]
오른쪽 아래: "위 셀의 값이 없어서 오류"
```

**프롬프트**

```
Two vertical columns on a white background, separated by a thin divider.

Left column: three stacked rounded rectangles representing notebook cells,
arranged neatly top to bottom, each with a small square badge on its left edge.
Light indigo #eef2ff fill, thin #e0e7ff borders. Calm and orderly.

Right column: the same three rounded rectangles in the same positions, but a
thick indigo #4f46e5 arrow weaves between them in a tangled out-of-order path —
starting at the bottom rectangle, curving up to the top one, then down to the
middle one. The arrow crosses over itself once. One rectangle is outlined in
amber #b45309 to mark a problem.

Flat vector diagram, thin consistent strokes, no shadows, generous white space.
no text, no letters, no numbers, no labels, no watermark
```

---

### 02-2-01-vscode-4 ★★★ (AI 생성 아님)

- **위치** — `02-2.html` · 4.2 VS Code에서 쓸 네 곳
- **캡션** — 그림 2-2-1. VS Code에서 쓰게 될 네 곳
- **목적** — 처음 VS Code를 여는 학생이 화면에서 길을 잃지 않게 한다.
- **판단** — **실제 화면 캡처를 써야 한다.** 생성 이미지는 실물과 달라 오히려 혼란을 준다.

**촬영 방법**

1. 실습에 쓸 노트북에서 `jdh-info` 폴더를 VS Code로 연다.
2. 왼쪽 탐색기를 펼치고, 아래쪽 터미널을 연 상태로 만든다.
3. 편집기에는 `notebooks/00_setup_test.ipynb`를 열어 둔다.
4. 화면 전체를 캡처한다. 창 크기는 **1600×900 이상**.
5. 다음 네 곳에 **번호 원(①②③④)과 테두리**를 얹는다.
   - ① 탐색기 Explorer
   - ② 편집기 Editor
   - ③ 확장 Extensions (왼쪽 아이콘 줄)
   - ④ 터미널 Terminal
6. 번호 색은 교과서 색과 맞춰 indigo `#4f46e5` / 흰 글자.

**주의**

- 캡처 전에 **파일 경로에 실명이 없는지** 확인한다. 사용자 폴더 이름이 그대로 찍히기 쉽다.
- 열려 있는 다른 탭, 알림, 확장 목록에 개인 정보가 없는지 본다.
- 교실 노트북과 **같은 테마**(기본 Dark 또는 Light)로 통일한다.

---

### 02-2-02-venv

- **위치** — `02-2.html` · 9. 가상환경이 필요한 나머지 세 가지 이유
- **캡션** — 그림 2-2-2. 환경 자체가 아니라 ‘환경을 만드는 기록’을 전달한다
- **목적** — `.venv`를 복사해 주는 것이 아니라 기록 파일을 전달해 재생성한다는, 학생이 가장 헷갈려 하는 지점을 보여 준다.
- **비율** — 16 : 9

**라벨** (방법 A로 얹을 것)

```
왼쪽 위: data-project/.venv → pandas, Matplotlib
왼쪽 아래: web-project/.venv → FastAPI, Uvicorn
가운데 작은 문서: pyproject.toml + uv.lock
오른쪽: 다른 노트북 — uv sync
가로지르는 X 표시 위: .venv 폴더는 전달하지 않는다
```

**프롬프트**

```
A left-to-right process diagram on a white background.

Left side: two separate rounded rectangle containers stacked vertically, each
holding two small pill shapes inside. The containers are clearly separate from
each other, with a visible gap. Light indigo #eef2ff fill, thin #e0e7ff borders.

Center: a small document icon with a folded corner, drawn in indigo #4f46e5.
A thick indigo arrow points from the left containers to this document, and
another arrow continues from the document to the right.

Right side: a simple laptop outline, and inside its screen a rounded rectangle
container being rebuilt — drawn with a dashed indigo outline to suggest
regeneration.

Below the center, a separate small container shape with a red #b91c1c diagonal
cross over it, indicating "not this one".

Flat vector diagram, thin consistent strokes, no shadows, generous white space.
no text, no letters, no numbers, no labels, no watermark
```

---

### 02-2-03-three-pythons ★★★

- **위치** — `02-2.html` · 13. 인터프리터와 커널 선택하기
- **캡션** — 그림 2-2-3. 세 곳이 모두 같은 `.venv`를 가리켜야 한다
- **목적** — **실습에서 가장 자주 실패하는 지점**이다. 세 곳의 설정이 하나로 모여야 한다는 것을 그림으로 못 박는다.
- **비율** — 16 : 9
- **판단** — **SVG 권장.** 세 갈래 이름이 모두 라벨이고, 인쇄에서 선명해야 한다.

**라벨**

```
위 세 개: 터미널 uv run python  /  VS Code 인터프리터  /  Notebook 커널
아래 하나: jdh-info/.venv
아래 작은 글씨: 세 화살표가 모두 한 곳을 가리켜야 한다
```

**프롬프트 (이미지로 만들 경우)**

```
A converging diagram on a white background. Three rounded rectangle boxes sit
in a row across the top, evenly spaced. From the bottom of each box, a thick
indigo #4f46e5 arrow curves downward and inward, all three meeting at a single
larger rounded rectangle centered below them. The bottom box is filled solid
indigo #4f46e5; the three top boxes are light indigo #eef2ff with thin
#e0e7ff borders. Symmetrical, balanced composition.
Flat vector diagram, thin consistent strokes, no shadows, generous white space.
no text, no letters, no numbers, no labels, no watermark
```

---

### 03-1-01-variable-label ★★★

- **위치** — `03-1.html` · 3. 변수는 값에 붙인 이름이다
- **캡션** — 그림 3-1-1. 변수는 상자가 아니라 값을 가리키는 이름표에 가깝다
- **목적** — 이 차시의 **오개념 1순위**를 정면으로 다룬다. 본문에서 “변수는 상자가 아니라 이름표”라고 말로 바로잡지만, 학생 머릿속에는 이미 상자 그림이 들어 있다. 말보다 그림이 필요한 자리다.
- **비율** — 16 : 9
- **주의** — **상자를 그리면 안 된다.** 값을 안에 담는 그림은 이 페이지가 바로잡으려는 오개념을 다시 심는다. 반드시 **바깥에서 화살표로 가리키는** 구도여야 한다.

**라벨** (방법 A로 얹을 것)

```
왼쪽 위 이름표: price      왼쪽 아래 값: 1200
오른쪽 위 이름표: price    오른쪽 아래 값: 1200(흐리게) / 3000(진하게)
가운데 구분선 위: price = 1200        →        price = 3000
아래 한 줄: 값이 이름표 안으로 들어가는 것이 아니라, 이름표가 값을 가리킨다
```

**프롬프트**

```
Two side-by-side panels on a white background, separated by a thin vertical divider.

Left panel: a small rounded tag shape with a short string, like a luggage tag,
positioned upper left. A thick indigo #4f46e5 arrow leaves the tag and points
down-right to a separate rounded rectangle containing a blank space. The tag and
the rectangle are clearly separate objects with a visible gap between them —
the tag is NOT a container and nothing is inside it.

Right panel: the same tag in the same position, but its arrow now points to a
different rounded rectangle placed lower right. The previously pointed rectangle
is still present but drawn faded in light gray #e4e4e7 with no arrow touching it.

Flat vector diagram, thin consistent strokes, no shadows, no boxes containing
other boxes, generous white space.
no text, no letters, no numbers, no labels, no watermark
```

## 4. 진행 체크리스트

| ID | 생성 | 라벨 | 압축 | 배치 | 인쇄 확인 |
|---|:--:|:--:|:--:|:--:|:--:|
| `00-01-course-map` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `01-1-01-literacy` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `01-2-01-human-computer` | ☑ | — | ☑ | ☑ | ☑ |
| `01-2-02-cli-gui` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `01-2-03-scope` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `02-1-01-notebook-order` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `02-2-01-vscode-4` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `02-2-02-venv` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `02-2-03-three-pythons` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `03-1-01-variable-label` | ☑ | ☑ | ☑ | ☑ | ☑ |
| `03-2-01-control-flow` | ☑ | — | ☑ | ☑ | ☐ |
| `03-2-02-list-index` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `04-1-01-chart-types` | ☑ | — | ☑ | ☑ | ☐ |
| `04-1-02-axis-trick` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `04-1-03-same-mean` | ☑ | ☑ | ☑ | ☑ | ☐ |
| `04-2-01-notebook-flow` | ☑ | — | ☑ | ☑ | ☐ |
| `05-2-01-submission-layout` | ☑ | ☑ | ☑ | ☑ | ☐ |

**인쇄 확인**은 브라우저에서 `🖨 인쇄`를 눌러 A4 미리보기로 본다. 화면에서 괜찮아도 인쇄에서 흐리거나 잘리는 경우가 있다.

---

## 5. 앞으로 추가할 그림

07-1 이후 차시를 집필하면서 이 문서에 계속 덧붙인다. ID 규칙은 `<차시>-<순번>-<짧은이름>`이다.

집필하며 그림이 필요하다고 판단하는 기준은 다음과 같다.

- 글로 두 문단 이상 설명해야 겨우 전달되는 관계나 구조인가?
- 학생이 **눈으로 본 적 없는 화면**인가? → 캡처
- **시간에 따른 변화나 순서**인가? → 흐름도
- 이미 본문의 `.stack`, `.vflow`, `.flow`, `.iot` 같은 CSS 컴포넌트로 충분히 표현되는가? → **그림을 만들지 않는다**

마지막 기준이 중요하다. 교과서 CSS에 도식용 컴포넌트가 이미 갖춰져 있으므로, 그림은 **CSS로 표현할 수 없는 것**에만 쓴다.
